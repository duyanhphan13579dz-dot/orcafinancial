/** Providers without TCBS — VNDirect and other public/licensed feeds only. */
export { getBreaker } from "@/lib/connectors/core";

// Re-export the rest of the original providers module body by reading local file is required.
// This placeholder will be replaced by the full cleaned providers file.
export async function placeholderRemovedTcbs() {
  return null;
}

# DEPRECATED — Grafana/Prometheus stack removed (not required for core product).
# Metrics endpoints /api/metrics and /api/v1/forex/metrics remain available if
# you wire your own scraper later. See git history for previous compose config.
#
# This file is kept empty on purpose so old docs/scripts do not fail loudly.
services: {}
